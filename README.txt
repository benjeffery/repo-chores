This tool iterates through all repos in an org, checking each one out, running an update then pushing to a fork and creating a PR.

Steering is done with a yaml file, see example.yaml.

Run with `python main.py --config file.yaml`
